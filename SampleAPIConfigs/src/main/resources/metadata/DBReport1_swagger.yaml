openapi: 3.0.1
info:
  title: DBReport1
  description: API Definition of DBReport1
  version: 1.0.0
servers:
- url: http://localhost:8290/DBReport1
paths:
  /:
    get:
      responses:
        default:
          description: Default response
    post:
      responses:
        default:
          description: Default response
