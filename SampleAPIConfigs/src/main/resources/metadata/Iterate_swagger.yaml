openapi: 3.0.1
info:
  title: Iterate
  description: API Definition of Iterate
  version: 1.0.0
servers:
- url: http://localhost:8290/iterate
paths:
  /:
    post:
      responses:
        default:
          description: Default response
